namespace = prospecting

### Dwarven Events ###
#			dwarven.0100		Erebor Starting Event



###################################
### Dwarven Introduction Events ###
###################################

prospecting.0001 = {
	type = character_event
	title = prospecting.0001.t
	desc = prospecting.0001.desc
	theme = crown

	right_portrait ={
		character = root
		animation = rage
	}

	trigger = {
		heritage_blacklock
		heritage_broadbeam
		heritage_firebeard
		heritage_longbeard
	}

	immediate = {
		add_piety = -100
	}

	option = {
		if ={
			limit = {has_trait = dwarf_father}
			add_piety = 100
		}
		name = prospecting.0001.a
		add_gold = 100
		
	}
	option = {
		trigger = {has_trait = generous}
		if ={
			limit = {has_trait = dwarf_father}
			add_piety = 200
		}
		name = prospecting.0001.b
		add_gold = 200
		
	}
	option = {
		trigger = {has_trait = generous}
		if ={
			random = {
			chance = 85
			root.primary_heir = {add_prestige = 100
			}
			}
			limit = {has_trait = dwarf_father}
			add_piety = 200
		}
		name = prospecting.0001.c
		add_gold = 150
		
	}
	after = {add_piety = -1000}
}
