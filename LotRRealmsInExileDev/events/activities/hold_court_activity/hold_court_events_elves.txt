hold_court.9001 = {
	type = court_event
	title = hold_court.9001.t
	desc = hold_court.9001.desc
	theme = court
	lower_center_portrait = cp:councillor_marchal
	lower_right_portrait = scope:character_a

	court_scene = {
		button_position_character = scope:rumormonger
		court_event_force_open = yes
		show_timeout_info = no
		should_pause_time = yes
		roles = {
			scope:character_a = {
				group = petitioners_group
				animation = fear
			}
			cp:councillor_marshal = {
				group = petitioners_group
				animation = marshal
			}
		}
	}

	widget = {
		gui = "event_window_widget_event_chain_progress"
		container = "custom_widgets_container"
		controller = event_chain_progress
	}

	trigger = {
		any_this_title_or_de_jure_above = {
			this = title:e_greenwood
		}
		any_pool_guest = {
			count >= 1
			is_available_adult = yes
			has_court_event_flag = no
		}
	}

	cooldown = { years = 10 }

	immediate = {
		# As with portraits, we do our general character setup stuff in this event.
		random_courtier_or_guest = { save_scope_as = character_a }
	}

	# Court events, as with all events, require at least one option to display.
	option = {
		name = hold_court.9001.a
		appoint_court_position = {
			recipient = scope:character_a
			court_position = cupbearer_court_position
		}
	}

	option = {
		name = hold_court.9001.b
		appoint_court_position = {
			recipient = scope:character_a
			court_position = court_jester_court_position
		}
		if = {
			limit = {
				any_court_position_holder = {
					has_court_position = court_jester_court_position
				}
			}
		}
		cp:court_jester = {
			add_opinion = 100
		}
	}

	option = {
		name = hold_court.9001.c
	}
}