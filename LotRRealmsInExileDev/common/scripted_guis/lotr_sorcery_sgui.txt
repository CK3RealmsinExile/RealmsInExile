lotr_death_spell_intent_selected = {
	effect = {
		if = {
			limit = { has_variable = spell_selected }
			remove_variable = spell_selected
		}
		set_variable = {
			name = spell_selected
			value = flag:death_spell
		}
	}
}

lotr_destruction_spell_intent_selected = {
	effect = {
		if = {
			limit = { has_variable = spell_selected }
			remove_variable = spell_selected
		}
		set_variable = {
			name = spell_selected
			value = flag:destruction_spell
		}
	}
}

lotr_deception_spell_intent_selected = {
	effect = {
		if = {
			limit = { has_variable = spell_selected }
			remove_variable = spell_selected
		}
		set_variable = {
			name = spell_selected
			value = flag:deception_spell
		}
	}
}