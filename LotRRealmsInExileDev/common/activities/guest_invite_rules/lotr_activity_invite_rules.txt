### ACTIVITY INVITE RULES ###

# Elven Rulers
activity_invite_other_elven_rulers = {
	effect = {
		if = {
			limit = { is_elf = yes }
			every_independent_ruler = {
				limit = {
					is_elf = yes
				}
				add_to_list = characters
			}
		}
	}
}
