contains_prospecting_modifier = {
	OR = {
		any_held_title = {
			title_province ?= { has_province_modifier = $PROSPECTING_MODIFIER$ }
		}
		any_vassal_or_below = {
			any_held_title = {
				title_province ?= { has_province_modifier = $PROSPECTING_MODIFIER$ }
			}
		}
	}
}