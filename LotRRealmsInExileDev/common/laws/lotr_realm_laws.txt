@orc_authority_cooldown_years = 20

orcish_government_authority = {
	default = orc_authority_1
	flag = realm_law
	flag = dominance_law

	orc_authority_1 = {
		modifier = {
			vassal_tax_contribution_mult = 0.05
			vassal_levy_contribution_mult = 0.05
			ai_boldness = 20
		}
		
		flag = uses_orc_authority
		flag = increased_factions
		flag = can_have_tributaries

		can_keep = {
			realm_law_use_orcish_government_authority = yes
		}

		can_pass = {
			has_realm_law = orc_authority_2
		}

		on_pass = {
			# Remove modifiers.
			remove_law_related_modifiers_effect = yes
		}
	}
	
	orc_authority_2 = {
		modifier = {
			barons_and_minor_landholders_opinion = -10
			glory_hound_opinion = -10
			parochial_opinion = -10
			courtly_opinion = -10
			zealot_opinion = -10
			minority_opinion = -10
			vassal_tax_contribution_mult = 0.10
			vassal_levy_contribution_mult = 0.10
			men_at_arms_limit = 1
			ai_boldness = 40
			ai_war_chance = 20
			dread_baseline_add = 10
		}
		
		flag = uses_orc_authority
		flag = increased_factions
		flag = can_have_tributaries
		flag = can_designate_heirs
		flag = title_revocation_allowed

		can_keep = {
			realm_law_use_orcish_government_authority = yes
		}

		can_pass = {
			custom_tooltip = {
				text = "orc_authority_2_level_requirement"
				OR = {
					has_realm_law = orc_authority_1
					has_realm_law = orc_authority_2
				}
			}
			trigger_if = {
				limit = { has_realm_law = orc_authority_1 }
				dread > 40
			}
			custom_description = {
				subject = root
				text = "has_orc_authority_cooldown"
				NAND = {
					has_variable = orc_authority_cooldown
					NOT = {
						culture = { has_innovation = innovation_all_things }
					}
				}
			}
		}
		
		pass_cost = {
			prestige = {
				if = {
					limit = {
						NOT = { has_realm_law = orc_authority_3 }
					}
					add = increase_orc_authority_prestige_cost
				}
			}
		}
		
		on_pass = {
			# Increment cooldown breaker tally, if appropriate.
			calculate_authority_cooldown_break_effect = yes
			# Set regular cooldown.
			set_variable = {
				name = orc_authority_cooldown
				years = @orc_authority_cooldown_years
			}
			# Remove modifiers.
			remove_law_related_modifiers_effect = yes
		}

		ai_will_do = {
			if = {
				limit = { has_realm_law = orc_authority_1 }
				value = 1
			}
		}
	}
	
	orc_authority_3 = {
		modifier = {
			barons_and_minor_landholders_opinion = -20
			glory_hound_opinion = -20
			parochial_opinion = -20
			courtly_opinion = -20
			zealot_opinion = -20
			minority_opinion = -20
			vassal_tax_contribution_mult = 0.15
			vassal_levy_contribution_mult = 0.15
			men_at_arms_limit = 1
			ai_boldness = 60
			ai_war_chance = 40
			dread_baseline_add = 20
		}
		
		flag = uses_orc_authority
		flag = increased_factions
		flag = can_have_tributaries
		flag = can_designate_heirs
		flag = title_revocation_allowed
		flag = vassal_retraction_allowed
		flag = titles_cannot_leave_realm_on_succession
		
		can_keep = {
			realm_law_use_orcish_government_authority = yes
			trigger_if = {
				limit = {
					government_allows = administrative
					is_independent_ruler = no
				}
				liege = { has_realm_law = orc_authority_3 }
			}
		}

		can_pass = {
			custom_tooltip = {
				text = "orc_authority_3_level_requirement"
				OR = {
					has_realm_law = orc_authority_2
					has_realm_law = orc_authority_4
				}
			}
			trigger_if = {
				limit = { has_realm_law = orc_authority_2 }
				dread > 60
			}
			custom_description = {
				subject = root
				text = "has_orc_authority_cooldown"
				NAND = {
					has_variable = orc_authority_cooldown
					NOT = {
						culture = { has_innovation = innovation_all_things }
					}
				}
			}
		}

		pass_cost = {
			prestige = {
				if = {
					limit = {
						NOT = { has_realm_law = orc_authority_4 }
					}
					add = increase_orc_authority_prestige_cost
				}
			}
		}
		
		on_pass = {
			# Increment cooldown breaker tally, if appropriate.
			calculate_authority_cooldown_break_effect = yes
			# Set regular cooldown.
			set_variable = {
				name = orc_authority_cooldown
				years = @orc_authority_cooldown_years
			}
			# Remove modifiers.
			remove_law_related_modifiers_effect = yes
		}

		ai_will_do = {
			if = {
				limit = { has_realm_law = orc_authority_2 }
				value = 1
			}
		}
	}

	orc_authority_4 = {
		modifier = {
			barons_and_minor_landholders_opinion = -35
			glory_hound_opinion = -35
			parochial_opinion = -35
			courtly_opinion = -35
			zealot_opinion = -35
			minority_opinion = -35
			vassal_tax_contribution_mult = 0.25
			vassal_levy_contribution_mult = 0.25
			men_at_arms_limit = 1
			men_at_arms_cap = 1
			ai_boldness = 80
			ai_war_chance = 60
			dread_baseline_add = 30
		}
		
		flag = uses_orc_authority
		flag = increased_factions
		flag = can_have_tributaries
		flag = can_designate_heirs
		flag = title_revocation_allowed
		flag = vassal_retraction_allowed
		flag = titles_cannot_leave_realm_on_succession
		flag = diarchs_want_to_subsidise_without_this_flag

		can_keep = {
			realm_law_use_orcish_government_authority = yes
		}

		can_pass = {
			custom_tooltip = {
				text = "orc_authority_4_level_requirement"
				OR = {
					has_realm_law = orc_authority_3
					has_realm_law = orc_authority_5
				}
			}
			trigger_if = {
				limit = { has_realm_law = orc_authority_3 }
				dread > 80
			}
			custom_description = {
				subject = root
				text = "has_orc_authority_cooldown"
				NAND = {
					has_variable = orc_authority_cooldown
					NOT = {
						culture = { has_innovation = innovation_all_things }
					}
				}
			}
		}
		
		pass_cost = {
			prestige = {
				if = {
					limit = {
						NOT = { has_realm_law = orc_authority_5 }
					}
					add = increase_orc_authority_prestige_cost
				}
			}
		}
		
		on_pass = {
			# Remove modifiers.
			remove_law_related_modifiers_effect = yes
		}

		ai_will_do = {
			if = {
				limit = { has_realm_law = orc_authority_3 }
				value = 1
			}
		}
	}
	
	orc_authority_5 = {
		modifier = {
			barons_and_minor_landholders_opinion = -50
			glory_hound_opinion = -50
			parochial_opinion = -50
			courtly_opinion = -50
			zealot_opinion = -50
			minority_opinion = -50
			vassal_tax_contribution_mult = 0.35
			vassal_levy_contribution_mult = 0.35
			men_at_arms_limit = 1
			men_at_arms_cap = 2
			ai_boldness = 100
			ai_war_chance = 80
			dread_baseline_add = 40
		}
		
		flag = uses_orc_authority
		flag = increased_factions
		flag = can_have_tributaries
		flag = can_designate_heirs
		flag = title_revocation_allowed
		flag = vassal_retraction_allowed
		flag = titles_cannot_leave_realm_on_succession
		flag = diarchs_want_to_subsidise_without_this_flag

		can_keep = {
			realm_law_use_orcish_government_authority = yes
		}

		can_pass = {
			custom_tooltip = {
				text = "orc_authority_4_level_requirement"
				has_realm_law = orc_authority_4
			}
			dread > 100
			custom_description = {
				subject = root
				text = "has_orc_authority_cooldown"
				NAND = {
					has_variable = orc_authority_cooldown
					NOT = {
						culture = { has_innovation = innovation_all_things }
					}
				}
			}
		}
		
		pass_cost = {
			prestige = {
				if = {
					limit = {
						NOT = { has_realm_law = orc_authority_5 }
					}
					add = increase_orc_authority_prestige_cost
				}
			}
		}
		
		on_pass = {
			# Increment cooldown breaker tally, if appropriate.
			calculate_authority_cooldown_break_effect = yes
			# Set regular cooldown.
			set_variable = {
				name = orc_authority_cooldown
				years = @orc_authority_cooldown_years
			}
			# Remove modifiers.
			remove_law_related_modifiers_effect = yes
		}

		ai_will_do = {
			if = {
				limit = { has_realm_law = orc_authority_4 }
				value = 1
			}
		}
	}
}