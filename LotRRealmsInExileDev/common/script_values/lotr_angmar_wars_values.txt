devastation_potential = { # Slighty Randomises choice so it's not just development level, and helps clean up borders
	value = 0
	add = {
		value = 1
		divide = {
			value = development_level
			min = 0.5
		}
	}
	every_neighboring_county = {
		limit = {
			prev.holder = { save_temporary_scope_as = county_holder }
			holder = { angmar_wars_is_opposite_side = { CHARACTER = scope:county_holder }}
		}
		multiply = 10
	}
	every_neighboring_county = {
		limit = {
			holder = title:k_wastelands.holder
		}
		multiply = 2
	}
	# multiply = { # to make it feel a bit more dynamic - Can't use fixed ranger in a 'trigger' apparently 
	# 	fixed_range = {
	# 		min = 1
	# 		max = 2
	# 	}
	# }
}