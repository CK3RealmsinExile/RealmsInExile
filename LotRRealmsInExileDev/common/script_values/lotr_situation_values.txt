# Elven Catalyst Values
minimal_elven_hope_value = 5
minor_elven_hope_value = 10
medium_elven_hope_value = 20
major_elven_hope_value = 30
massive_elven_hope_value = 40
monumental_elven_hope_value = 50

elven_situation_zero = 0
elven_fading_limit = 200
elven_waning_limit = 400
elven_enduring_limit = 600
elven_renewing_limit = 800
elven_thriving_limit = 1000

# Mirkwood Corruption Values
corruption_situation_zero = 0
corruption_untainted_limit = 100
corruption_whispers_in_the_leaves_limit = 200
corruption_withering_shade_limit = 300
corruption_shadows_grasp_limit = 400
corruption_web_of_night_limit = 500


# Context: situation_sub_region
mkw_is_active_value = {
    value = 0

    # identify WHICH sub-region this GUI item is (by geo membership),
    # then apply your per-region active checks.
    if = {
        limit = { any_province = { geographical_region = custom_aradhrynd } }
        if = {
            limit = { mkw_aradhrynd_is_active = yes }
            add = 1
        }
    }
    if = {
        limit = { any_province = { geographical_region = custom_taurduin } }
        if = {
            limit = { mkw_taurduin_is_active = yes }
            add = 1
        }
    }
    if = {
        limit = { any_province = { geographical_region = custom_taur_nu_fuin } }
        if = {
            limit = { mkw_taurnufuin_is_active = yes }
            add = 1
        }
    }
    if = {
        limit = { any_province = { geographical_region = custom_emyn_i_firith } }
        if = {
            limit = { mkw_emynifirith_is_active = yes }
            add = 1
        }
    }
    if = {
        limit = { any_province = { geographical_region = custom_nivrim } }
        if = {
            limit = { mkw_nivrim_is_active = yes }
            add = 1
        }
    }
    if = {
        limit = { any_province = { geographical_region = custom_dundaedelu } }
        if = {
            limit = { mkw_dundaedelu_is_active = yes }
            add = 1
        }
    }
    if = {
        limit = { any_province = { geographical_region = custom_angaladh_rhunen } }
        if = {
            limit = { mkw_angaladhrhunen_is_active = yes }
            add = 1
        }
    }
    if = {
        limit = { any_province = { geographical_region = custom_men_i_naugrim } }
        if = {
            limit = { mkw_meninaugrim_is_active = yes }
            add = 1
        }
    }
    if = {
        limit = { any_province = { geographical_region = custom_gwath_anglennol } }
        if = {
            limit = { mkw_gwathanglennol_is_active = yes }
            add = 1
        }
    }
    if = {
        limit = { any_province = { geographical_region = custom_taur_e_ndaedlos } }
        if = {
            limit = { mkw_taurendaedlos_is_active = yes }
            add = 1
        }
    }
    if = {
        limit = { any_province = { geographical_region = custom_amon_lanc } }
        if = {
            limit = { mkw_amonlanc_is_active = yes }
            add = 1
        }
    }
}
