story_cycle_winter = {
	
	on_setup = { }

	on_end = {
	}

	on_owner_death = { end_story = yes }

	effect_group = { #Set the appropriate modifiers on the unifier of Khand
		days = 1
		# trigger = { title:c_kingstead.holder = { realm_has_any_epidemic = { SIZE = 1 INTENSITY = minor } }}
		triggered_effect = {
			effect = {
				title:c_framsburg.title_province = { 
					create_epidemic_outbreak = {
						type = long_winter_plague	
						intensity = major
						save_scope_as = winter
					}
				}
			}
		}
	}
}