pool_tamska_succession = {
	valid_character = {
		#is_imprisoned = no
		#is_female = yes
		#scope:base.faith = { has_allowed_gender_for_clergy = root }
		dynasty = scope:base.dynasty
		#OR = { # We want kids or capables
		#	is_capable_adult = yes
		#	is_adult = no
		#}
	}
	
	character_score = {
		base = 1
		
		modifier = { # Above all, the Tamska Heir
			add = 6000
			has_trait = tamska_heir
		}
		modifier = { # Next, kids of the current Tamska
			add = 2000
			is_child_of = scope:base
		}
		modifier = { # Prefer any born after she ascended to Tamska
			add = 1000
			has_trait = child_of_tamska
		}
		
		
		# If theres no clear successor, determine the one most qualified for the job
		modifier = { # Adults first
			add = 200
			is_capable_adult = yes
		}
		modifier = {
			add = {
				value = num_virtuous_traits
				multiply = 10
			}
		}
		modifier = {
			add = {
				value = num_sinful_traits
				multiply = -10
			}
		}
		modifier = {
			add = {
				value = sum_of_all_skills_value
				multiply = 3
			}
		}
		modifier = {
			piety_level >= 2
			add = {
				value = piety_level
				multiply = 5
			}
		}
		modifier = {
			add = 30
			has_same_culture_as = scope:base
		}
		modifier = {
			add = 20
			culture = { has_same_culture_heritage = scope:base.culture }
		}
		modifier = {
			add = 5
			culture = { has_same_culture_language = scope:base.culture }
		}
		modifier = {
			add = 5
			culture = { has_same_culture_ethos = scope:base.culture }
		}
		modifier = {
			add = -50
			age > 35
		}
	}
	
	config = {
		#background = commander
		age = { 1 45 }
	}
	
	#selection_count = 25
	
	pool = faith
	gender = gender_law
	#court = yes
}