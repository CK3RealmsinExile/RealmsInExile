#### Situation Effects ####

devestate_county_effect = { # Fires on county scope, takes paramter $SEVERITY$, which quantifies the destruction.
	while = { #First do random raid damage
		count = $SEVERITY$.div2
		devestate_county_secondary_effect = yes # Pass through to selection effect
	}
	while = { # Then systematically ruin the county.
		count = $SEVERITY$
		devestate_county_repeated_effect = yes # Pass through to selection effect
	}
	save_scope_as = devestated_county
	angmar_wars_catalyst_angmar_devestated_effect = yes 
	angmar_wars_catalyst_arnor_devestated_effect = yes 
	holder = { #Fire notification for players associated to devestation
		send_interface_toast = {
			type = event_toast_effect_bad
			title = county_devestated_toast
			custom_tooltip = county_devestated_toast_desc
		}
		every_liege_or_above = {
			limit = {
				exists = this
				# is_ai = no # Players
			}
			send_interface_toast = {
				type = event_toast_effect_bad
				title = county_devestated_toast
				custom_tooltip = county_devestated_toast_desc
			}
		}
	}
	# error_log = "ANGMAR TEST - County Devestated: $SEVERITY$"
}

devestate_county_repeated_effect = { # Fires on county scope, removes one level each time fired
	if = { # First, reduce development
		limit = {
			development_level > 0
		}
		change_development_level = -1
	}
	else_if = { # If there's not enough dev, start on buildings
		limit = {
			any_county_province = {
				has_holding = yes
				lotr_normal_building_levels > 0
			}
		}
		ordered_county_province = {
			order_by = combined_building_level.inverse # Take least built up place (that has enough levels) first
			limit = {
				has_holding = yes
				lotr_normal_building_levels > 0
			}
			destroy_random_building_variable_effect = yes
			destroy_random_building_effect = yes
			error_log = "TEST - destroyed building"
		}
	}
	else_if = { # If no province has buildings, start taking holding levels in outer provinces.
		limit = {
			any_county_province = {
				has_holding = yes
				is_county_capital = no # Can't destroy the capital holding yet
			}
		}
		ordered_county_province = {
			order_by = lotr_holding_level.inverse # Take least built up place first
			limit = {
				has_holding = yes
				is_county_capital = no
			}
			if = {
				limit = {
					lotr_holding_level > 1
				}
				lotr_downgrade_holding = yes
				error_log = "TEST - downgraded holding"
			}
			else = {
				remove_holding = yes
				error_log = "TEST - removed holding"
			}
		}
	}
	else_if = { #Now start reducing the capital holding
		limit = {
			title_province = {
				lotr_holding_level > 1
			}
		}
		title_province = {
			lotr_downgrade_holding = yes
			error_log = "TEST - downgraded capital holding"
		}
	}
	else_if = { # If down to just one level of holding in capital, convert to wilderness
		limit = {
			title_province = {
				lotr_holding_level = 1
			}
		}
		make_settlement_county_wilderness = { COUNTY = this }
		error_log = "TEST - wastelanded county"
	}
}

devestate_county_secondary_effect = {
	random_list = {
		40 = {} # Nothing happens

		5 = { # Rarely take an extra dev
			if = {
				limit = {
					development_level > 0
				}
				change_development_level = -1
			}
		}
		10 = { # Steal some money
			holder = {
				remove_short_term_gold = miniscule_gold_value
			}
		}
		45 = { # Destroy a building
			if = {
				limit = {
					any_county_province = {
						has_holding = yes
						lotr_normal_building_levels > 0
					}
				}
				ordered_county_province = {
					order_by = combined_building_level.inverse # Take least built up place first
					limit = {
						has_holding = yes
						lotr_normal_building_levels > 0
					}
					destroy_random_building_variable_effect = yes
					destroy_random_building_effect = yes
				}
			}
		}
	}
}

devestate_random_sitatuation_county = { # Run from situation scope, effects up to MAXIMUM counties
	ordered_situation_county = {
		limit = {
			holder = {
				save_temporary_scope_as = target_county_holder
			}
			any_neighboring_county = {
				holder = {
					OR = {
						is_wastelands = yes
						angmar_wars_is_opposite_side = { CHARACTER = scope:target_county_holder }
					}
				}
			}
			holder = {
				is_elven_ruler = no
			}
		}
		order_by = devestation_potential
		max = 4
		check_range_bounds = no

		devestate_county_effect = { SEVERITY = 1 }
	}
}

#### Catalyst Effects ####

angmar_wars_catalyst_convert_to_evil_effect = {
	if = {
		limit = {
			is_in_angmar_wars = yes
			faith_is_evil = yes
		}

		situation:angmar_wars_situation = {
			trigger_situation_catalyst = {
				catalyst = catalyst_convert_to_evil
				character = root
			}
		}
	}
	
}

angmar_wars_catalyst_convert_to_good_effect = {
	if = {
		limit = {
			is_in_angmar_wars = yes
			faith_is_good = yes
		}
		situation:angmar_wars_situation = {
			trigger_situation_catalyst = {
				catalyst = catalyst_convert_to_good
				character = root
			}
		}
	}
}

angmar_wars_catalyst_angmar_devestated_effect = { # County scope
	if = {
		limit = {
			county.holder = {
				is_in_angmar_wars = yes
				is_angmar_aligned = yes
			}
		}
		situation:angmar_wars_situation = {
			trigger_situation_catalyst = {
				catalyst = catalyst_angmar_devestated
				county = scope:devestated_county
			}
		}
	}
}

angmar_wars_catalyst_arnor_devestated_effect = { # County scope
	if = {
		limit = {
			county.holder = {
				is_in_angmar_wars = yes
				is_arnor_aligned = yes
			}
		}
		situation:angmar_wars_situation = {
			trigger_situation_catalyst = {
				catalyst = catalyst_arnor_devestated
				county = scope:devestated_county
			}
		}
	}
}

angmar_wars_catalyst_arnor_won_war_effect = {
	if = {
		limit = {
			is_in_angmar_wars = yes
			is_arnorian_ruler = yes
		}
		save_scope_as = war_winner
		situation:angmar_wars_situation = {
			trigger_situation_catalyst = {
				catalyst = catalyst_arnor_won_war
				character = scope:war_winner
			}
		}
	}
}

angmar_wars_catalyst_angmar_won_war_effect = {
	if = {
		limit = {
			is_in_angmar_wars = yes
			is_angmar_ruler = yes
		}
		save_scope_as = war_winner
		situation:angmar_wars_situation = {
			trigger_situation_catalyst = {
				catalyst = catalyst_angmar_won_war
				character = scope:war_winner
			}
		}
	}
}