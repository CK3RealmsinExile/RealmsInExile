decision_ray_decide_on_allies = { ### ray decides to ally itself to the stormhost - what challenges should there be with allying itself to them
	title = "title"
	desc = "desc"
	confirm_text = "confirm_text"
	selection_tooltip = "selection_tooltip"
	picture = "gfx/interface/illustrations/decisions/decision_shire_01.dds"
	sort_order = 5
	major = yes

	# Shown
	is_shown = {
		is_landed = yes
		has_title = title:k_ray
		NOT = { has_title = title:e_bozisha_miraz } # adding this here because if player controls bozisha miraz, they wouldnt consider allying with storm-host
		exists = title:e_storm_host
		NOT = {
			is_target_in_global_variable_list = {
				name = unavailable_unique_decisions
				target = flag:side_with_stormhost_as_ray
			}
		}
	}

	#Requirements
	is_valid = {
		is_ruler = yes
		completely_controls = title:k_ray
		NOT = { culture = culture:wastelands }
	}

	# Effect
	effect = {
		add_to_global_variable_list = {
			name = unavailable_unique_decisions
			target = flag:side_with_stormhost_as_ray
		}
		title:k_ray.holder = {
			save_scope_as = king_of_ray
		}
		title:e_storm_host.holder = {
			save_scope_as = storm_marshal
		}
		title:e_bellakar.holder = {
			save_scope_as = emperor_of_bellekar
		}
		trigger_event = {
			id = ray.0001
		}
	}

	#Cost
	cost = {
		prestige = major_prestige_value
		piety = major_piety_value
	}

	# AI
	ai_goal = yes
	ai_potential = {
		always = yes
	}
	ai_will_do = {
		base = 100
	}
}






# ray decides to ally itself with Bellekar - what challenges should there be with allying itself to them
#
#
#
#
#
#
#
#

