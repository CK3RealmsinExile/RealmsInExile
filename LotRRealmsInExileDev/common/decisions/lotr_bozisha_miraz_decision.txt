


decision_reclaim_ray = { # Reclaim the Ray for the Honnin, pushing out the Haradrim invaders!
	title = decision_reclaim_ray
	picture = "gfx/interface/illustrations/decisions/decision_honnin_reclamation.dds"
	major = yes
	ai_goal = yes
	ai_potential = {
		always = yes
	}
	ai_will_do = {
		base = 100
	}
	selection_tooltip = decision_reclaim_ray_tooltip
	desc = decision_reclaim_ray_desc
	confirm_text = decision_reclaim_ray_confirm
	
	is_shown = {
		is_landed = yes
		has_culture = culture:honnin
		NOT = {
			is_target_in_global_variable_list = {
				name = unavailable_unique_decisions
				target = flag:reclaim_ray_decision
				}
			}
		}
	is_valid = {
		completely_controls_region = custom_greater_ray
		prestige_level >= high_prestige_level
		is_independent_ruler = yes
	}
	
	is_valid_showing_failures_only = {
		is_imprisoned = no
		is_available_adult = yes
		is_at_war = no
	}
	
	effect = {
		if = {
			limit = { has_government = tribal_government }
			change_government = feudal_government
			add_realm_law_skip_effects = crown_authority_1
			get_all_innovations_from = culture:bozishnarud
			add_prestige_level = 2
			add_dynasty_prestige = 500
			give_nickname = nick_honnin_restorer
			#every_realm_province = {
			#	limit = {
			#		has_culture_group = culture_group:lotr_rayan_group
			#	}
			#	add_province_modifier = honnin_resettlements
			#}
		}
		else_if = {
			limit = { 
				OR = {
					has_government = clan_government
					has_government = feudal_government
				}
			}
			get_all_innovations_from = culture:bozishnarud
			add_prestige_level = 2
			add_dynasty_prestige = 500
			give_nickname = nick_honnin_restorer
			add_realm_law_skip_effects = crown_authority_3
			add_piety = 1000
			add_prestige = 1000
			change_development_progress_with_overflow = {
				root.title_capital_county = 750
			}
			#every_realm_province = {
			#	limit = {
			#		has_culture_group = culture_group:lotr_rayan_group
			#	}
			#	add_province_modifier = honnin_resettlements
			#}
		}
		
		add_to_global_variable_list = {
			name = unavailable_unique_decisions
			target = flag:reclaim_ray_decision
		}
		save_scope_as = honnin_restorer
	}
	
	ai_will_do = {
		base = 100
	}
}