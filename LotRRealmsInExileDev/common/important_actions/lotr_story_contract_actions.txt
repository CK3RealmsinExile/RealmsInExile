action_story_contract_available = {
	type = alert
	check_create_action = {
		any_owned_story = { story_type = story_cycle_bountig_medlokan }
		random_task_contract = {
			limit = { has_task_contract_type = lotr_bountig_story }
			try_create_important_action = {
				important_action_type = action_story_contract_available
				actor = root
				recipient = this
			}
		}
	}

	effect = {
		scope:recipient = {
			random_task_contract = {
				limit = { has_task_contract_type = lotr_bountig_story }
				pan_camera_to_province = task_contract_employer.location
			}	
		}
		root = { open_view_data = task_contract }
	}
}