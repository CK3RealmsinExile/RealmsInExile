# Hold of Belegost #
wonder_belegost_broken = { # Belegost (Broadbeam Capital)
	type_icon = "icon_structure_newgrange.dds"

	#We only build this through Decision -> Great Projects
	can_construct_potential = {
		always = no #This should never show up
	}

	is_enabled = {
		is_dwarf = yes
	}

	show_disabled = yes

	rebuild_cost = {
		gold = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { value = standard_rebuild_value }
		}
		piety = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { value = standard_rebuild_value }
		}
	}

	can_rebuild = {
		NOT = { has_building_with_flag = desecrated_dwarven_capital }
		is_dwarf = yes
		has_building_or_higher = dwarven_depths_01
	}
	
	province_modifier = {
		supply_limit_mult = -0.5
	}
	
	province_terrain_modifier = {
 		tax_mult = -0.20
 	}

	character_modifier = {
		monthly_prestige = 0.25
		stress_gain_mult = 0.1
		monthly_crafting_lifestyle_xp_gain_mult = 0.10
	}

	fallback = {
		province_modifier = {
			tax_mult = -0.20
		}
	}
	
	type = great_building
	flag = travel_point_of_interest_economic
	flag = first_tier_dwarven_capital_building
	flag = dwarven_capital_building
	next_building = wonder_belegost_t2
}
wonder_belegost_t2 = { # Belegost (Broadbeam Capital)
	type_icon = "icon_structure_newgrange.dds"
	effect_desc = feast_cost_discount_max_desc

	#We only build this through Decision -> Great Projects
	can_construct_potential = {
		is_dwarf = yes
		has_building_or_higher = dwarven_depths_02
	    lotr_dwarven_underground_buildings_requirement_terrain = yes
		has_building = wonder_belegost_broken
	}
	is_enabled = {
		is_dwarf = yes
	}
	can_rebuild = {
		NOT = { has_building_with_flag = desecrated_dwarven_capital }
		is_dwarf = yes
	}

	show_disabled = yes

	rebuild_cost = {
		gold = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { 
				value = {
					add = standard_rebuild_value
					multiply = 1.25
				}
			}
		}
		piety = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { 
				value = {
					add = standard_rebuild_value
					multiply = 1.25
				}
			}
		}
	}

	province_modifier = {
		monthly_income = 1
		stationed_heavy_infantry_damage_mult = 0.25
		stationed_heavy_infantry_toughness_mult = 0.25
	}
	county_modifier = {
		development_growth = 0.1
	}
	character_modifier = {
		monthly_prestige = 0.5
		monthly_crafting_lifestyle_xp_gain_mult = 0.15
	}
	
	fallback = {
		province_modifier = {
			monthly_income = 0.1
		}
	}

	type = great_building
	great_project_type = dwarven_capital_02
	flag = travel_point_of_interest_economic
	next_building = wonder_belegost_rebuilt
	flag = dwarven_capital_building
	flag = second_tier_dwarven_capital_building
}
wonder_belegost_rebuilt = { # Belegost (Broadbeam Capital)
	type_icon = "icon_structure_newgrange.dds"
	effect_desc = feast_cost_discount_max_desc

	#We only build this through Decision -> Great Projects
	can_construct_potential = {
		is_dwarf = yes
		has_building_or_higher = dwarven_depths_04
	    lotr_dwarven_underground_buildings_requirement_terrain = yes
		has_building = wonder_belegost_t2
	}
	is_enabled = {
		is_dwarf = yes
	}
	can_rebuild = {
		NOT = { has_building_with_flag = desecrated_dwarven_capital }
		is_dwarf = yes
	}

	show_disabled = yes

	rebuild_cost = {
		gold = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { 
				value = {
					add = standard_rebuild_value
					multiply = 1.5
				}
			}
		}
		piety = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { 
				value = {
					add = standard_rebuild_value
					multiply = 1.5
				}
			}
		}
	}

	province_modifier = {
		monthly_income = 2
		stationed_heavy_infantry_damage_mult = 0.5
		stationed_heavy_infantry_toughness_mult = 0.5
	}
	county_modifier = {
		development_growth = 0.2
	}
	character_modifier = {
		monthly_prestige = 1
		monthly_crafting_lifestyle_xp_gain_mult = 0.30
	}
	
	fallback = {
		province_modifier = {
			monthly_income = 0.1
		}
	}

	type = great_building
	great_project_type = dwarven_capital_03
	flag = travel_point_of_interest_economic
	next_building = wonder_belegost_t4
	flag = dwarven_capital_building
	flag = third_tier_dwarven_capital_building
}
wonder_belegost_t4 = { # Belegost (Broadbeam Capital)
	type_icon = "icon_structure_newgrange.dds"
	effect_desc = feast_cost_discount_max_desc

	#We only build this through Decision -> Great Projects
	can_construct_potential = {
		is_dwarf = yes
		has_building_or_higher = dwarven_depths_06
	    lotr_dwarven_underground_buildings_requirement_terrain = yes
		has_building = wonder_belegost_rebuilt
	}
	is_enabled = {
		is_dwarf = yes
	}
	can_rebuild = {
		NOT = { has_building_with_flag = desecrated_dwarven_capital }
		is_dwarf = yes
	}

	show_disabled = yes

	rebuild_cost = {
		gold = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { 
				value = {
					add = standard_rebuild_value
					multiply = 1.75
				}
			}
		}
		piety = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { 
				value = {
					add = standard_rebuild_value
					multiply = 1.75
				}
			}
		}
	}

	province_modifier = {
		monthly_income = 3.5
		stationed_heavy_infantry_damage_mult = 1
		stationed_heavy_infantry_toughness_mult = 1
		fort_level = normal_building_fort_level_tier_1
	}
	county_modifier = {
		development_growth = 0.3
	}
	character_modifier = {
		monthly_prestige = 1.5
		monthly_crafting_lifestyle_xp_gain_mult = 0.4
		same_culture_opinion = 10
		stress_loss_mult = 0.05
	}
	
	fallback = {
		province_modifier = {
			monthly_income = 0.1
		}
	}

	type = great_building
	great_project_type = dwarven_capital_04
	flag = travel_point_of_interest_economic
	next_building = wonder_belegost_reborn
	flag = dwarven_capital_building
	flag = fourth_tier_dwarven_capital_building
}
wonder_belegost_reborn = { # Belegost (Broadbeam Capital)
	type_icon = "icon_structure_newgrange.dds"
	effect_desc = {
		desc = feast_cost_discount_max_desc
		desc = unlocks_broadbeam_dragonslayers_recruitment_desc
	}

	#We only build this through Decision -> Great Projects
	can_construct_potential = {
		is_dwarf = yes
		has_building_or_higher = dwarven_depths_08
	    lotr_dwarven_underground_buildings_requirement_terrain = yes
		has_building = wonder_belegost_t4
	}
	is_enabled = {
		is_dwarf = yes
	}
	can_rebuild = {
		NOT = { has_building_with_flag = desecrated_dwarven_capital }
		is_dwarf = yes
	}

	show_disabled = yes

	rebuild_cost = {
		gold = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { 
				value = {
					add = standard_rebuild_value
					multiply = 2
				}
			}
		}
		piety = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { 
				value = {
					add = standard_rebuild_value
					multiply = 2
				}
			}
		}
	}

	province_modifier = {
		monthly_income = 5
		stationed_heavy_infantry_damage_mult = 1.5
		stationed_heavy_infantry_toughness_mult = 1.5
		fort_level = normal_building_fort_level_tier_3
	}
	county_modifier = {
		development_growth = 0.4
	}
	character_modifier = {
		monthly_prestige = 2
		same_culture_opinion = 20
		stress_loss_mult = 0.1
		monthly_crafting_lifestyle_xp_gain_mult = 0.50
	}

	fallback = {
		province_modifier = {
			monthly_income = 0.1
		}
	}

	type = great_building
	flag = travel_point_of_interest_economic
	great_project_type = dwarven_capital_05
	flag = dwarven_capital_building
	flag = final_tier_dwarven_capital_building
}

# Hold of Nogrod #
wonder_nogrod_broken = { # Nogrod (Firebeard Capital)
	type_icon = "icon_structure_walls_of_genoa.dds"

	#We only build this through Decision -> Great Projects
	can_construct_potential = {
		always = no #This should never show up
	}

	is_enabled = {
		is_dwarf = yes
	}

	show_disabled = yes

	rebuild_cost = {
		gold = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { value = standard_rebuild_value }
		}
		piety = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { value = standard_rebuild_value }
		}
	}

	can_rebuild = {
		NOT = { has_building_with_flag = desecrated_dwarven_capital }
		is_dwarf = yes
		has_building_or_higher = dwarven_depths_01
	}

	province_modifier = {
		supply_limit_mult = -0.5
	}
	
	province_terrain_modifier = {
 		tax_mult = -0.30
 	}

	character_modifier = {
		stress_gain_mult = 0.1
		monthly_prestige = 0.25
		monthly_crafting_lifestyle_xp_gain_mult = 0.10
	}


	fallback = {
		province_modifier = {
			tax_mult = -0.30
		}
	}
	
	type = great_building
	flag = travel_point_of_interest_martial
	flag = first_tier_dwarven_capital_building
	flag = dwarven_capital_building
	next_building = wonder_nogrod_t2
}
wonder_nogrod_t2 = { # Nogrod (Firebeard Capital)
	type_icon = "icon_structure_newgrange.dds"
	effect_desc = feast_cost_discount_max_desc

	can_construct_potential = {
		is_dwarf = yes
		has_building_or_higher = dwarven_depths_02
	    lotr_dwarven_underground_buildings_requirement_terrain = yes
		has_building = wonder_nogrod_broken
	}
	is_enabled = {
		is_dwarf = yes
	}
	can_rebuild = {
		NOT = { has_building_with_flag = desecrated_dwarven_capital }
		is_dwarf = yes
	}

	show_disabled = yes

	rebuild_cost = {
		gold = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { 
				value = {
					add = standard_rebuild_value
					multiply = 1.25
				}
			}
		}
		piety = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { 
				value = {
					add = standard_rebuild_value
					multiply = 1.25
				}
			}
		}
	}

	province_modifier = {
		supply_limit_mult = 0.15
		monthly_income = 1.75
	}
	county_modifier = {
		development_growth = 0.1
	}
	character_modifier = {
		monthly_prestige = 0.5
		monthly_crafting_lifestyle_xp_gain_mult = 0.15
	}

	fallback = {
		province_modifier = {
			monthly_income = 0.1
		}
	}

	type = great_building
	great_project_type = dwarven_capital_02
	flag = travel_point_of_interest_martial	
	next_building = wonder_nogrod_rebuilt
	flag = dwarven_capital_building
	flag = second_tier_dwarven_capital_building
}
wonder_nogrod_rebuilt = { # Nogrod (Firebeard Capital)
	type_icon = "icon_structure_newgrange.dds"
	effect_desc = feast_cost_discount_max_desc

	can_construct_potential = {
		is_dwarf = yes
		has_building_or_higher = dwarven_depths_04
	    lotr_dwarven_underground_buildings_requirement_terrain = yes
		has_building = wonder_nogrod_t2
	}
	is_enabled = {
		is_dwarf = yes
	}
	can_rebuild = {
		NOT = { has_building_with_flag = desecrated_dwarven_capital }
		is_dwarf = yes
	}

	show_disabled = yes

	rebuild_cost = {
		gold = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { 
				value = {
					add = standard_rebuild_value
					multiply = 1.5
				}
			}
		}
		piety = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { 
				value = {
					add = standard_rebuild_value
					multiply = 1.5
				}
			}
		}
	}

	province_modifier = {
		supply_limit_mult = 0.25
		monthly_income = 3.5
	}
	county_modifier = {
		development_growth = 0.2
	}
	character_modifier = {
		monthly_prestige = 1
		monthly_crafting_lifestyle_xp_gain_mult = 0.30
	}

	fallback = {
		province_modifier = {
			monthly_income = 0.1
		}
	}

	type = great_building
	great_project_type = dwarven_capital_03
	flag = travel_point_of_interest_martial	
	next_building = wonder_nogrod_t4
	flag = dwarven_capital_building
	flag = third_tier_dwarven_capital_building
}
wonder_nogrod_t4 = { # Nogrod (Firebeard Capital)
	type_icon = "icon_structure_newgrange.dds"
	
	effect_desc = {
		desc = feast_cost_discount_max_desc
		desc = unlocks_firebeard_monsters_recruitment_desc
	}

	#We only build this through Decision -> Great Projects
	can_construct_potential = {
		is_dwarf = yes
		has_building_or_higher = dwarven_depths_06
	    lotr_dwarven_underground_buildings_requirement_terrain = yes
		has_building = wonder_nogrod_rebuilt
	}
	is_enabled = {
		is_dwarf = yes
	}
	can_rebuild = {
		NOT = { has_building_with_flag = desecrated_dwarven_capital }
		is_dwarf = yes
	}

	show_disabled = yes

	rebuild_cost = {
		gold = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { 
				value = {
					add = standard_rebuild_value
					multiply = 1.75
				}
			}
		}
		piety = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { 
				value = {
					add = standard_rebuild_value
					multiply = 1.75
				}
			}
		}
	}

	province_modifier = {
		supply_limit_mult = 0.25
		monthly_income = 4
		stationed_heavy_infantry_damage_mult = 0.75
		stationed_heavy_infantry_toughness_mult = 0.75
		fort_level = normal_building_fort_level_tier_1
	}
	county_modifier = {
		development_growth = 0.2
	}
	character_modifier = {
		stress_loss_mult = 0.05
		monthly_prestige = 1
		same_culture_opinion = 10
		monthly_crafting_lifestyle_xp_gain_mult = 0.25
	}

	fallback = {
		province_modifier = {
			monthly_income = 0.1
		}
	}

	type = great_building
	flag = travel_point_of_interest_martial
	great_project_type = dwarven_capital_04
	next_building = wonder_nogrod_reborn
	flag = dwarven_capital_building
	flag = fourth_tier_dwarven_capital_building
}
wonder_nogrod_reborn = { # Nogrod (Firebeard Capital)
	type_icon = "icon_structure_newgrange.dds"
	
	effect_desc = {
		desc = feast_cost_discount_max_desc
		desc = unlocks_firebeard_monsters_recruitment_desc
	}

	#We only build this through Decision -> Great Projects
	can_construct_potential = {
		is_dwarf = yes
		has_building_or_higher = dwarven_depths_08
	    lotr_dwarven_underground_buildings_requirement_terrain = yes
		has_building = wonder_nogrod_t4
	}
	is_enabled = {
		is_dwarf = yes
	}
	can_rebuild = {
		NOT = { has_building_with_flag = desecrated_dwarven_capital }
		is_dwarf = yes
	}

	show_disabled = yes

	rebuild_cost = {
		gold = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { 
				value = {
					add = standard_rebuild_value
					multiply = 2
				}
			}
		}
		piety = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { 
				value = {
					add = standard_rebuild_value
					multiply = 2
				}
			}
		}
	}

	province_modifier = {
		supply_limit_mult = 0.5
		monthly_income = 5
		stationed_heavy_infantry_damage_mult = 1.5
		stationed_heavy_infantry_toughness_mult = 1.5
		fort_level = normal_building_fort_level_tier_3
	}
	county_modifier = {
		development_growth = 0.4
	}
	character_modifier = {
		stress_loss_mult = 0.1
		monthly_prestige = 2
		same_culture_opinion = 20
		monthly_crafting_lifestyle_xp_gain_mult = 0.50
	}

	fallback = {
		province_modifier = {
			monthly_income = 0.1
		}
	}

	type = great_building
	flag = travel_point_of_interest_martial
	great_project_type = dwarven_capital_05
	flag = dwarven_capital_building
	flag = final_tier_dwarven_capital_building
}

### YELLOW MOUNTAINS ###

wonder_mabladdum_broken = { # Mablad-dum (destroyed by volcano)
	type_icon = "icon_structure_krakatoa.dds"

	#We only build this through Decision -> Great Projects
	can_construct_potential = {
		always = no #This should never show up
	}

	is_enabled = {
		is_dwarf = yes
	}

	show_disabled = yes

	rebuild_cost = {
		gold = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { value = standard_rebuild_value }
		}
		piety = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { value = standard_rebuild_value }
		}
	}

	can_rebuild = {
		NOT = { has_building_with_flag = desecrated_dwarven_capital }
		is_dwarf = yes
		has_building_or_higher = dwarven_depths_01
	}
	
	province_modifier = {
		supply_limit_mult = -0.5
	}
	
	province_terrain_modifier = {
 		tax_mult = -0.20
 	}

	character_modifier = {
		monthly_prestige = 0.25
		stress_gain_mult = 0.1
	}
	
	fallback = {
		province_modifier = {
			tax_mult = -0.2
		}
	}
	
	type = great_building
	flag = travel_point_of_interest_martial
	flag = first_tier_dwarven_capital_building
	next_building = wonder_mabladdum_rebuilt
}
wonder_mabladdum_t2 = { # Mablad-dum
	type_icon = "icon_structure_newgrange.dds"
	effect_desc = feast_cost_discount_max_desc

	can_construct_potential = {
		is_dwarf = yes
		has_building_or_higher = dwarven_depths_02
	    lotr_dwarven_underground_buildings_requirement_terrain = yes
		has_building = wonder_mabladdum_broken
	}
	is_enabled = {
		is_dwarf = yes
	}
	can_rebuild = {
		NOT = { has_building_with_flag = desecrated_dwarven_capital }
		is_dwarf = yes
	}

	show_disabled = yes

	rebuild_cost = {
		gold = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { 
				value = {
					add = standard_rebuild_value
					multiply = 1.25
				}
			}
		}
		piety = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { 
				value = {
					add = standard_rebuild_value
					multiply = 1.25
				}
			}
		}
	}

	province_modifier = {
		supply_limit_mult = 0.07
		monthly_income = 1
	}
	county_modifier = {
		development_growth = 0.05
	}
	character_modifier = {
		monthly_prestige = 0.25
		monthly_crafting_lifestyle_xp_gain_mult = 0.07
	}

	fallback = {
		province_modifier = {
			monthly_income = 0.1
		}
	}

	type = great_building
	great_project_type = dwarven_capital_02
	flag = travel_point_of_interest_economic	
	next_building = wonder_mabladdum_rebuilt
	flag = dwarven_capital_building
	flag = second_tier_dwarven_capital_building
}
wonder_mabladdum_rebuilt = { # Mablad-dum
	type_icon = "icon_structure_great_fire_temple.dds"

	can_construct_potential = {
		is_dwarf = yes
		has_building_or_higher = dwarven_depths_04
	    lotr_dwarven_underground_buildings_requirement_terrain = yes
	}
	is_enabled = {
		is_dwarf = yes
	}
	can_rebuild = {
		NOT = { has_building_with_flag = desecrated_dwarven_capital }
		is_dwarf = yes
	}

	show_disabled = yes

	rebuild_cost = {
		gold = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { 
				value = {
					add = standard_rebuild_value
					multiply = 1.5
				}
			}
		}
		piety = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { 
				value = {
					add = standard_rebuild_value
					multiply = 1.5
				}
			}
		}
	}

	province_modifier = {
		tax_mult = 0.1
		stationed_maa_damage_mult = 0.5
		stationed_maa_toughness_mult = 0.5
	}
	county_modifier = {
		development_growth = 0.3
	}
	character_modifier = {
		monthly_prestige_gain_per_happy_powerful_vassal_add = 0.1
		monthly_crafting_lifestyle_xp_gain_mult = 0.25
	}

	# Make feasts cost less?

	fallback = {
		province_modifier = {
			monthly_income = 0.1
		}
	}

	type = great_building
	great_project_type = dwarven_capital_03
	flag = travel_point_of_interest_economic	
	next_building = wonder_mabladdum_t4
	flag = dwarven_capital_building
	flag = third_tier_dwarven_capital_building
}
wonder_mabladdum_t4 = { # Mablad-dum
	type_icon = "icon_structure_great_fire_temple.dds"

	#We only build this through Decision -> Great Projects
	can_construct_potential = {
		is_dwarf = yes
		has_building_or_higher = dwarven_depths_06
	    lotr_dwarven_underground_buildings_requirement_terrain = yes
		has_building = wonder_nogrod_rebuilt
	}
	is_enabled = {
		is_dwarf = yes
	}
	can_rebuild = {
		NOT = { has_building_with_flag = desecrated_dwarven_capital }
		is_dwarf = yes
	}

	show_disabled = yes

	rebuild_cost = {
		gold = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { 
				value = {
					add = standard_rebuild_value
					multiply = 1.75
				}
			}
		}
		piety = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { 
				value = {
					add = standard_rebuild_value
					multiply = 1.75
				}
			}
		}
	}

	province_modifier = {
		tax_mult = 0.15
		stationed_maa_damage_mult = 1
		stationed_maa_toughness_mult = 1
	}
	county_modifier = {
		development_growth = 0.5
	}
	character_modifier = {
		monthly_prestige_gain_per_happy_powerful_vassal_add = 0.15
		same_culture_opinion = 10
		monthly_dynasty_prestige_mult = 0.05
		monthly_crafting_lifestyle_xp_gain_mult = 0.5
	}

	fallback = {
		province_modifier = {
			monthly_income = 0.1
		}
	}

	type = great_building
	flag = travel_point_of_interest_economic
	great_project_type = dwarven_capital_04
	next_building = wonder_mabladdum_reborn
	flag = dwarven_capital_building
	flag = fourth_tier_dwarven_capital_building
}
wonder_mabladdum_reborn = { # Mablad-dum
	type_icon = "icon_structure_great_fire_temple.dds"

	#We only build this through Decision -> Great Projects
	can_construct_potential = {
		is_dwarf = yes
		has_building_or_higher = dwarven_depths_08
	    lotr_dwarven_underground_buildings_requirement_terrain = yes
		has_building = wonder_mabladdum_t4
	}
	is_enabled = {
		is_dwarf = yes
	}
	can_rebuild = {
		NOT = { has_building_with_flag = desecrated_dwarven_capital }
		is_dwarf = yes
	}

	show_disabled = yes

	rebuild_cost = {
		gold = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { 
				value = {
					add = standard_rebuild_value
					multiply = 2
				}
			}
		}
		piety = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { 
				value = {
					add = standard_rebuild_value
					multiply = 2
				}
			}
		}
	}

	province_modifier = {
		tax_mult = 0.2
		stationed_maa_damage_mult = 1.5
		stationed_maa_toughness_mult = 1.5
	}
	county_modifier = {
		development_growth = 0.8
	}
	character_modifier = {
		monthly_prestige_gain_per_happy_powerful_vassal_add = 0.2
		same_culture_opinion = 20
		monthly_dynasty_prestige_mult = 0.1
		monthly_crafting_lifestyle_xp_gain_mult = 0.5
		stewardship_per_prestige_level = 1
	}

	fallback = {
		province_modifier = {
			monthly_income = 0.1
		}
	}

	type = great_building
	flag = travel_point_of_interest_economic
	great_project_type = dwarven_capital_05
	flag = dwarven_capital_building
	flag = final_tier_dwarven_capital_building
}

# Throne of Durin #
wonder_durin_throne_broken = { # Throne of Durin (Longbeard Capital)
	type_icon = "icon_wonder_thranduil_hall.dds"

	#We only build this through Decision -> Great Projects
	can_construct_potential = {
		always = no #This should never show up
	}

	is_enabled = {
		is_dwarf = yes
	}

	show_disabled = yes

	rebuild_cost = {
		gold = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { value = standard_rebuild_value }
		}
		piety = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { value = standard_rebuild_value }
		}
	}

	can_rebuild = {
		NOT = { has_building_with_flag = desecrated_dwarven_capital }
		is_dwarf = yes
		has_building_or_higher = dwarven_depths_01
	}

	province_modifier = {
		supply_limit_mult = -0.5
	}
	
	province_terrain_modifier = {
 		tax_mult = -0.30
 	}

	character_modifier = {
		stress_gain_mult = 0.1
		monthly_dynasty_prestige_mult = 0.1
	}


	fallback = {
		province_modifier = {
			tax_mult = -0.30
		}
	}
	
	type = great_building
	flag = travel_point_of_interest_diplomatic
	flag = first_tier_dwarven_capital_building
	flag = dwarven_capital_building
	next_building = wonder_durin_throne_t2
}
wonder_durin_throne_t2 = {
	type_icon = "icon_wonder_thranduil_hall.dds"

	can_construct_potential = {
		is_dwarf = yes
		has_building_or_higher = dwarven_depths_02
	    lotr_dwarven_underground_buildings_requirement_terrain = yes
		has_building = wonder_durin_throne_broken
	}
	is_enabled = {
		is_dwarf = yes
	}
	can_rebuild = {
		NOT = { has_building_with_flag = desecrated_dwarven_capital }
		is_dwarf = yes
	}

	show_disabled = yes

	rebuild_cost = {
		gold = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { 
				value = {
					add = standard_rebuild_value
					multiply = 1.25
				}
			}
		}
		piety = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { 
				value = {
					add = standard_rebuild_value
					multiply = 1.25
				}
			}
		}
	}

	province_modifier = {
		tax_mult = 0.1
		development_growth_factor = 0.25
	}

	character_modifier = {
		legitimacy_gain_mult = 0.1
		monthly_dynasty_prestige_mult = 0.1
		monthly_prestige = 0.5
		vassal_opinion = 5
		vassal_levy_contribution_add = 0.05
		vassal_tax_contribution_add = 0.05
		monthly_crafting_lifestyle_xp_gain_mult = 0.1
	}
	
	fallback = {
		province_modifier = {
			monthly_income = 0.1
		}
	}

	type = great_building
	great_project_type = dwarven_capital_02
	flag = travel_point_of_interest_diplomatic	
	next_building = wonder_durin_throne_t3
	flag = dwarven_capital_building
	flag = second_tier_dwarven_capital_building
}
wonder_durin_throne_t3 = {
	type_icon = "icon_wonder_thranduil_hall.dds"

	can_construct_potential = {
		is_dwarf = yes
		has_building_or_higher = dwarven_depths_04
	    lotr_dwarven_underground_buildings_requirement_terrain = yes
		has_building = wonder_durin_throne_t2
	}
	is_enabled = {
		is_dwarf = yes
	}
	can_rebuild = {
		NOT = { has_building_with_flag = desecrated_dwarven_capital }
		is_dwarf = yes
	}

	show_disabled = yes

	rebuild_cost = {
		gold = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { 
				value = {
					add = standard_rebuild_value
					multiply = 1.5
				}
			}
		}
		piety = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { 
				value = {
					add = standard_rebuild_value
					multiply = 1.5
				}
			}
		}
	}

	province_modifier = {
		tax_mult = 0.1
		development_growth_factor = 0.25
		monthly_income = 3.5
	}
	county_modifier = {
		development_growth = 0.2
	}
	character_modifier = {
		legitimacy_gain_mult = 0.2
		monthly_dynasty_prestige_mult = 0.2
		monthly_prestige = 1
		vassal_opinion = 5
		vassal_levy_contribution_add = 0.1
		vassal_tax_contribution_add = 0.1
		monthly_crafting_lifestyle_xp_gain_mult = 0.2
	}

	fallback = {
		province_modifier = {
			monthly_income = 0.1
		}
	}

	type = great_building
	great_project_type = dwarven_capital_03
	flag = travel_point_of_interest_martial	
	next_building = wonder_durin_throne_t4
	flag = dwarven_capital_building
	flag = third_tier_dwarven_capital_building
}
wonder_durin_throne_t4 = {
	type_icon = "icon_wonder_thranduil_hall.dds"

	#We only build this through Decision -> Great Projects
	can_construct_potential = {
		is_dwarf = yes
		has_building_or_higher = dwarven_depths_06
	    lotr_dwarven_underground_buildings_requirement_terrain = yes
		has_building = wonder_durin_throne_t3
	}
	is_enabled = {
		is_dwarf = yes
	}
	can_rebuild = {
		NOT = { has_building_with_flag = desecrated_dwarven_capital }
		is_dwarf = yes
	}

	show_disabled = yes

	rebuild_cost = {
		gold = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { 
				value = {
					add = standard_rebuild_value
					multiply = 1.75
				}
			}
		}
		piety = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { 
				value = {
					add = standard_rebuild_value
					multiply = 1.75
				}
			}
		}
	}

	province_modifier = {
		tax_mult = 0.1
		development_growth_factor = 0.25
		supply_limit_mult = 0.25
		monthly_income = 4
		fort_level = normal_building_fort_level_tier_1
	}
	county_modifier = {
		development_growth = 0.3
	}
	character_modifier = {
		legitimacy_gain_mult = 0.3
		monthly_dynasty_prestige_mult = 0.3
		monthly_prestige = 1
		vassal_opinion = 15
		vassal_levy_contribution_add = 0.15
		vassal_tax_contribution_add = 0.15
		monthly_crafting_lifestyle_xp_gain_mult = 0.3
	}

	fallback = {
		province_modifier = {
			monthly_income = 0.1
		}
	}

	type = great_building
	flag = travel_point_of_interest_martial
	great_project_type = dwarven_capital_04
	next_building = wonder_durin_throne_t5
	flag = dwarven_capital_building
	flag = fourth_tier_dwarven_capital_building
}
wonder_durin_throne_t5 = {
	type_icon = "icon_wonder_thranduil_hall.dds"
	
	effect_desc = {
		desc = feast_cost_discount_max_desc
		desc = unlocks_firebeard_monsters_recruitment_desc
	}

	#We only build this through Decision -> Great Projects
	can_construct_potential = {
		is_dwarf = yes
		has_building_or_higher = dwarven_depths_08
	    lotr_dwarven_underground_buildings_requirement_terrain = yes
		has_building = wonder_durin_throne_t4
	}
	is_enabled = {
		is_dwarf = yes
	}
	can_rebuild = {
		NOT = { has_building_with_flag = desecrated_dwarven_capital }
		is_dwarf = yes
	}

	show_disabled = yes

	rebuild_cost = {
		gold = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { 
				value = {
					add = standard_rebuild_value
					multiply = 2
				}
			}
		}
		piety = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { 
				value = {
					add = standard_rebuild_value
					multiply = 2
				}
			}
		}
	}

	province_modifier = {
		tax_mult = 0.5
		development_growth_factor = 0.5
		supply_limit_mult = 0.5
		monthly_income = 5
		fort_level = normal_building_fort_level_tier_3
	}
	county_modifier = {
		development_growth = 0.4
	}
	character_modifier = {
		legitimacy_gain_mult = 0.5
		monthly_dynasty_prestige_mult = 0.5
		monthly_prestige = 1.5
		vassal_opinion = 120
		vassal_levy_contribution_add = 0.2
		vassal_tax_contribution_add = 0.2
		monthly_crafting_lifestyle_xp_gain_mult = 0.5
	}

	fallback = {
		province_modifier = {
			monthly_income = 0.1
		}
	}

	type = great_building
	flag = travel_point_of_interest_martial
	great_project_type = dwarven_capital_05
	flag = dwarven_capital_building
	flag = final_tier_dwarven_capital_building
}

### Great Fleetport ###
lotr_great_fleet_01 = {
	type_icon = "icon_building_great_fleet.dds"
	
	effect_desc = {
		desc = sailing_activity_cost_discount_major_desc
	}

	can_construct_potential = {
		always = no # This building is the result of a great project
	}
	#cost_gold = expensive_building_tier_2_cost

	show_disabled = yes

	rebuild_cost = {
		gold = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { 
				value = {
					add = standard_rebuild_value
					multiply = 2
				}
			}
		}
		piety = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { 
				value = {
					add = standard_rebuild_value
					multiply = 2
				}
			}
		}
	}

	province_modifier = {
		tax_mult = 0.2
		development_growth_factor = 0.2
		supply_limit_mult = 0.2
		monthly_income = 1
		stationed_maa_pursuit_add = 10
	}
	# county_modifier = {
		# development_growth = 0.1
	# }
	character_modifier = {
		legitimacy_gain_mult = 0.2
		monthly_dynasty_prestige_mult = 0.1
		#monthly_dynasty_prestige = 0.02
		monthly_prestige = 0.5
		naval_movement_speed_mult = 0.25
		embarkation_cost_mult = -0.25
		supply_duration = 2
		floodplains_advantage = 5
	}

	# fallback = {
		# province_modifier = {
			# monthly_income = 0.1
		# }
	# }

	type = great_building
	flag = travel_point_of_interest_economic
	great_project_type = construct_great_fleet
	#flag = dwarven_capital_building
	#flag = final_tier_dwarven_capital_building
}

### Kinn-lai Reunification Building ###
kinnlai_spire_of_unity_01 = {
	type_icon = "icon_wonder_numenorean_tower.dds"

	can_construct_potential = {
		always = no # This building is the result of a decision
	}

	show_disabled = yes
	is_enabled = {
		is_elf = yes
	}

	rebuild_cost = {
		gold = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { 
				value = {
					add = standard_rebuild_value
					multiply = 2
				}
			}
		}
		piety = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { 
				value = {
					add = standard_rebuild_value
					multiply = 2
				}
			}
		}
	}

	province_modifier = {
		development_growth_factor = 0.1
		supply_limit_mult = 0.2
		monthly_income = 0.5
		stationed_maa_pursuit_add = 30
		stationed_skirmishers_toughness_mult = 0.8
		stationed_skirmishers_damage_mult = 0.8
		stationed_elephant_cavalry_damage_mult = 0.2 
		stationed_elephant_cavalry_toughness_mult = 0.2 
	}
	# county_modifier = {
		# development_growth = 0.1
	# }
	character_modifier = {
		legitimacy_gain_mult = 0.2
		monthly_dynasty_prestige_mult = 0.1
		#monthly_dynasty_prestige = 0.01
		monthly_prestige = 0.5
		monthly_piety = 0.5
	}

	# fallback = {
		# province_modifier = {
			# monthly_income = 0.1
		# }
	# }

	type = great_building
	flag = travel_point_of_interest_religious
	#next_building = kinnlai_spire_of_unity_02
}
# kinnlai_spire_of_unity_02 = {
	# type_icon = "icon_wonder_numenorean_tower.dds"

	# can_construct = {
		# scope:holder ?= {
			# is_elf = yes
		# }
		# building_requirement_elven = yes
		# has_building_or_higher = elven_settlement_03
	# }

	# show_disabled = yes
	# is_enabled = {
		# is_elf = yes
	# }

	# rebuild_cost = {
		# gold = {
			# if = {
				# limit = { is_ai = yes }
				# value = ai_rebuild_value
			# }
			# else = { 
				# value = {
					# add = standard_rebuild_value
					# multiply = 2
				# }
			# }
		# }
		# piety = {
			# if = {
				# limit = { is_ai = yes }
				# value = ai_rebuild_value
			# }
			# else = { 
				# value = {
					# add = standard_rebuild_value
					# multiply = 2
				# }
			# }
		# }
	# }

	# province_modifier = {
		# development_growth_factor = 0.2
		# supply_limit_mult = 0.3
		# monthly_income = 0.75
		# stationed_maa_pursuit_add = 30
		# stationed_skirmishers_toughness_mult = 5
		# stationed_skirmishers_damage_mult = 5
	# }
	# # county_modifier = {
		# # development_growth = 0.1
	# # }
	# character_modifier = {
		# legitimacy_gain_mult = 0.4
		# monthly_dynasty_prestige_mult = 0.15
		# monthly_dynasty_prestige = 0.02
		# monthly_prestige = 0.75
		# monthly_piety = 0.75
	# }

	# # fallback = {
		# # province_modifier = {
			# # monthly_income = 0.1
		# # }
	# # }

	# type = great_building
	# flag = travel_point_of_interest_religious
	# flag = fully_upgraded
# }

# City of Osgiliath #
wonder_osgiliath_01 = {
	type_icon = "icon_wonder_osgiliath.dds"

	can_construct = {
		always = no
		}

	show_disabled = yes

	rebuild_cost = {
		gold = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { value = standard_rebuild_value }
		}
		piety = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { value = standard_rebuild_value }
		}
	}

	county_modifier = {
		tax_mult = -0.3
		levy_size = -0.3
	}
	
	type = great_building
	flag = travel_point_of_interest_martial_minor
	flag = wonder_osgiliath_01
	next_building = wonder_osgiliath_02
}

wonder_osgiliath_02 = {
	type_icon = "icon_wonder_osgiliath.dds"

	can_construct = {
		 	always = no
		}

	show_disabled = yes

	rebuild_cost = {
		gold = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { value = standard_rebuild_value }
		}
		piety = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { value = standard_rebuild_value }
		}
	}

	
	county_modifier = {
		fort_level = good_building_fort_level_tier_1
		defender_holding_advantage = normal_building_advantage_tier_1
		tax_mult = -0.1
		levy_size = -0.1
	}

	type = great_building
	great_project_type = construct_osgiliath_02
	flag = travel_point_of_interest_martial_minor
	flag = wonder_osgiliath_02
	next_building = wonder_osgiliath_03
}

wonder_osgiliath_03 = {
	type_icon = "icon_wonder_osgiliath.dds"

	can_construct = {
		 	always = no
		}

	show_disabled = yes

	rebuild_cost = {
		gold = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { value = standard_rebuild_value }
		}
		piety = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { value = standard_rebuild_value }
		}
	}
	
	county_modifier = {
		fort_level = good_building_fort_level_tier_1
		defender_holding_advantage = normal_building_advantage_tier_1
		tax_mult = 0.1
		levy_size = 0.2
		supply_limit_mult = 0.3
		development_growth_factor = 0.05
	}

	type = great_building
	great_project_type = construct_osgiliath_03
	flag = wonder_osgiliath_03
	next_building = wonder_osgiliath_04
	flag = travel_point_of_interest_economic
}

wonder_osgiliath_04 = {
	type_icon = "icon_wonder_osgiliath.dds"

	can_construct = {
		 	always = no
		}

	is_enabled = {
		culture = { has_cultural_pillar = heritage_dunedain }
	}

	show_disabled = yes

	rebuild_cost = {
		gold = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { value = standard_rebuild_value }
		}
		piety = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { value = standard_rebuild_value }
		}
	}
	
	county_modifier = {
		fort_level = good_building_fort_level_tier_2
		defender_holding_advantage = normal_building_advantage_tier_2
		tax_mult = 0.3
		levy_size = 0.3
		supply_limit_mult = 0.5
		development_growth_factor = 0.15
	}

	type = great_building
	great_project_type = construct_osgiliath_04
	flag = wonder_osgiliath_04
	next_building = wonder_osgiliath_05
	flag = travel_point_of_interest_economic
}

wonder_osgiliath_05 = {
	type_icon = "icon_wonder_osgiliath.dds"

	effect_desc = explore_history_cost_discount_max_desc
	can_construct = {
		 	always = no
		}

	is_enabled = {
		culture = { has_cultural_pillar = heritage_dunedain }
	}

	show_disabled = yes

	rebuild_cost = {
		gold = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { value = standard_rebuild_value }
		}
		piety = {
			if = {
				limit = { is_ai = yes }
				value = ai_rebuild_value
			}
			else = { value = standard_rebuild_value }
		}
	}
	
	county_modifier = {
		fort_level = good_building_fort_level_tier_2
		defender_holding_advantage = normal_building_advantage_tier_2
		tax_mult = 0.5
		levy_size = 0.5
		supply_limit_mult = 0.8
		development_growth_factor = 0.25
	}

	type = great_building
	great_project_type = construct_osgiliath_05
	flag = wonder_osgiliath_05
	flag = travel_point_of_interest_economic
}